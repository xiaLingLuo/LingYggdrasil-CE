# 用户权限系统

用户权限组控制用户端（35565）功能的可用性，与[管理员权限系统](../03-admin/permissions.md)相互独立。

Root 账户不隶属于任何权限组，始终拥有全部权限。

> 权限组的创建与编辑在管理后台完成，见[管理后台指南](../03-admin/guide.md#权限组)。

## 权限列表

| 分类 | 权限键            | 说明                                   |
|------|-------------------|----------------------------------------|
| 访问 | `user.accessible` | 是否允许访问用户端（未授予则整体拒绝） |
| 功能 | `user.profiles`   | 角色管理                               |
| 功能 | `user.skins`      | 皮肤管理                               |
| 功能 | `user.capes`      | 披风管理                               |
| 功能 | `user.friends`    | 好友系统                               |
| 功能 | `user.world`      | 材质库（世界树）                       |
| 功能 | `user.settings`   | 账户设置                               |

### 生效范围

功能节点在服务端强制生效：缺少对应节点的用户访问该功能页面会被重定向到仪表盘，调用其接口会返回 403。

- `user.profiles`：角色页，及角色 / Token 相关接口。
- `user.skins` / `user.capes`：皮肤 / 披风页，及上传、删除、别名、可见性接口。
- `user.friends`：好友页，及好友管理、好友共享接口。
- `user.world`：共享材质页，及点赞、收藏等个人操作；`/world` 公共浏览不受限制。
- `user.settings`：设置页、日志页，及昵称 / 邮箱 / 密码等账户接口。

主题 / 语言切换、纹理图片下载（`/api/skins/download`、`/api/capes/download`）等跨功能只读接口不受功能节点限制。

## 内置用户组

| 组名      | 权限                  | 说明                   |
|-----------|-----------------------|------------------------|
| `default` | `*`（通配，全部权限） | 默认用户组             |
| `banned`  | 无                    | 封禁组，不可修改或删除 |

> `user.accessible` 是用户端的总开关。若某用户所在组未授予该权限，则无法访问用户端及其 Yggdrasil 认证。

## 动态权限节点

权限节点由来源（source）动态注册，权限组编辑页面按**来源分组**展示：

- 内置节点来源为 `LingYggdrasil`，组内再按分类细分。
- 插件可通过 `plugin.yml` 的 `perms` 声明自己的节点，来源为插件的 `friendlyName`（未设置时用 `name`），直接平铺展示。

插件节点即使未声明也仍然有效，只是不会在此列出。详见[插件开发手册](../07-plugin-dev/guide.md#5-权限节点声明)。

## 使用建议

- 对普通用户，若需关闭某项功能（如材质库），从 `default` 组中移除对应权限，或为其分配自定义组。
- 定期审查权限组成员。
